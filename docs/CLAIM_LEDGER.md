# Claim Ledger

Use this ledger to keep agents honest about what the repo actually knows. A
claim is not prize-facing just because it has a large run behind it.

## Levels

| Level | Meaning | Merge standard |
|---|---|---|
| Observation | One experiment or exploratory score. | Record setup, command, result, and next check. |
| Robust observation | Controls/nulls and at least one robustness check. | Include verification, baselines, and failure mode. |
| Certificate | Finite artifact another agent can verify. | Include verifier command and artifact path. |
| Theorem | Proved outright, no experiment needed. | Include the proof or a one-line derivation. |
| Proof candidate | Theorem-shaped argument with checkable lemmas. | Include assumptions, proof gaps, and reproduction path. |
| **Retracted** | Was recorded at a higher level and should not have been. | State what was wrong and what replaces it. |

## Admission Rule (added 2026-08-15)

Before a negative result ("we searched class `M` and found no fit") is recorded
at any level, state `log2|M|` and the prefix length `n`. If `log2|M| < n` the
result is **vacuous by counting** and must not be recorded as a Certificate — any
sequence whatsoever produces it. See
[`docs/theory/finite-prefix-counting-bound.md`](theory/finite-prefix-counting-bound.md)
and `python experiments/counting_bound.py --pretty`.

A matching negative from the random control is a **red flag that the experiment
is vacuous**, not a reassuring baseline.

## Current Claims

| Claim | Level | Evidence | What would promote it |
|---|---|---|---|
| Packed/GPU Rule 30 kernels are usable for research runs. | Robust observation | `docs/GPU_KERNELS.md`, reference checks in `experiments/rule30_open_utils.py`, fused-kernel branch validation. | A tiny standalone verifier that runs naive vs packed vs GPU across seeded edge cases in one command. |
| b=2 coarse-grain closure for Rule 30 is generic chaotic leakage, not Rule-30-specific reducibility. | Robust observation | `docs/experiment-logs/2026-06-13-coarse-grain-same-statistics-null.md`, `data/coarse_grain_rule_null.json`. | Independent rerun on multiple fields/radii, plus archived command output. |
| b=3 coarse-grain search does not find Rule-30-specific reducibility at r=1 and tested shears. | Robust observation | `docs/experiment-logs/2026-06-14-b3-coarse-grain-verdict.md`, `data/coarse_grain_b3_verdict.json`. | Multi-seed confidence band, larger `r`, alternate optimizers, and saved best projection artifacts. |
| The center column behaves statistically close to fair/random over tested prefixes. | Observation | README **A-H** summary and historical experiment logs. Scoped down from A-L on 2026-08-29: the I-L neural runs are withdrawn (row below). | Reframe as prize-specific discrepancy bounds or finite certificates, not more aggregate randomness tests. |
| ~~The first 128 center-column bits are not generated by any 1-4 state binary DFAO in MSD or LSD digit order.~~ | **Retracted** (was: Certificate) | `docs/theory/finite-prefix-counting-bound.md` | **Vacuous.** There are at most `2^28.2` behaviours in the 1-5 state class, so the expected number of fits on 128 bits is `2^-99.8`. A random sequence gives the identical negative — as the run's own seeded-random baseline in fact did. Replaced by the `s*(n)` curve row below. |
| Rule 30 is left-permutive; hence rightward damage speed is exactly 1 and the uniform Bernoulli(1/2) measure is invariant. | Theorem | ANF `a(t,i-1) ⊕ a(t,i) ⊕ a(t,i+1) ⊕ a(t,i)·a(t,i+1)`; `docs/theory/README.md` §1. | Nothing — already proved. Do not spend runs re-measuring either fact. |
| A finite-prefix negative over model class `M` is informative only when `log2\|M\| >= n`. | Theorem | Union bound; `docs/theory/finite-prefix-counting-bound.md`; `experiments/counting_bound.py`. | Nothing — already proved. Apply it before proposing searches. |
| The left diagonals satisfy the closed recursion `D_d(t+1) = D_{d-2}(t) ⊕ (D_{d-1}(t) ∨ D_d(t))`; hence every left diagonal is eventually periodic. | Theorem | Substitution into the Rule 30 update + induction from `D_0≡1, D_1≡1`; verified bit-exact by `python experiments/diagonal_recursion.py`; `docs/theory/README.md` §3. | Nothing — proved. The open part is the period-16 bound below. |
| ~~Every left diagonal is eventually periodic with period **dividing 16**.~~ | **Refuted 2026-08-19** (was: Proof candidate) | `docs/experiment-logs/2026-08-19-period16-refuted.md`, `data/wedge/period16_walk_1e6.json`. Diagonal d=87867 has minimal period **32**, confirmed at two independent simulation sizes (T=151375 and T=300000). Trigger: collision `w_87864 = w_87865 = 0x28a8`, popcount 5 (**odd**), zero word at d=87866 - Lemmas A and B both fired exactly as proved. | Refuted; nothing to promote. Replaced by the growth law below. |
| The period of left diagonal `d` grows as **`period(d) ~ 2*log2(d)`**, rounded down to a power of two - unbounded, but only logarithmically. | Robust observation | Doubling points 2, 7, 28, 399, 87866 track `2^(2^(k-1))` = 2, 4, 16, 256, 65536. Mechanism: doubling needs a `p`-bit collision (rate `2^-p`, measured) with odd parity (prob ~1/2), so period `2^k` starts near `d ~ 2^(2^(k-1))`. Period histogram over `d < 10^6`: `{1:16, 2:10, 4:56, 8:668, 16:87136, 32:912114}`. | Confirm the 32->64 event predicted near `d ~ 8.6e9`. Needs the generalized pattern map, not more direct simulation (`T ~ 1.2e10`). |
| Lemma B's parity must be taken at the **minimal period**, not over a padded 16-bit word. | Theorem | A period-`p` word padded to 16 bits is `16/p` copies, doubling its popcount and forcing even parity whenever `16/p` is even. This defect produced the false claim that the four early zero words had even-parity predecessors and 'doubling never fired'; at their true periods all four are odd and all four doubled. Fixed in `experiments/period_doubling.py`. | Nothing - proved. |
| The period-doubling branch fires exactly at a consecutive-word collision (`w_d = 0` iff `w_{d-2} = w_{d-1}`), and doubles iff `parity(w_{d-2})` is odd. | Theorem | Lemmas A and B in `experiments/period_doubling.py`; one-line proofs in `docs/experiment-logs/2026-08-19-period-doubling-criterion.md`. Verified: exhaustive at period 8 (65280 pairs, 0 violations), 200k sampled at period 16, and 0 violations against simulation over d=0..11999. | Nothing - proved. Use it to replace "bound how often the branch fires" with the two checkable conditions. |
| The settled words `w_d` past `d=403` are statistically indistinguishable from uniform 16-bit words; the `2^-16` collision rate underpinning the period-16 power analysis is **measured**, not assumed. | Robust observation | `docs/experiment-logs/2026-08-19-settled-word-genericity.md`, `data/wedge/word_genericity.json`. n=52802 words over `403 < d < 53205`, 0 mismatches vs simulation on the 11597-word overlap; 200 matched random controls, empirical p-values, Bonferroni over 5 statistics. All-pairs collision rate 1.518e-05 vs modelled 1.526e-05 (z=-0.73, p=0.48) across 1.39e9 pairs. Nothing significant. | Nothing statistical - the rate is pinned to 0.5%. The binding constraint is now structural. |
| The state map `(u,v) -> (v,w)` is a **bijection** wherever `v != 0`, via `u[t] = w[t+1] XOR (v[t] OR w[t])` (Lemma C). | Theorem | One-line inversion of the recursion; verified on 199995 random pairs, 0 failures. `docs/experiment-logs/2026-08-19-settled-word-genericity.md`. | Nothing - proved. Consequence: the functional graph is a disjoint union of simple paths and cycles, so period-16 is a reachability question on a 2^32-node graph with in/out-degree <= 1, not only a sampling question. |
| The pattern map is a **partial** map: `(w_{d-2}, w_{d-1})` is not a complete state, because at a zero word two period-16 words satisfy the recursion and only the (unboundedly long) diagonal transient picks one. Hence cycle-detection and exhaustive-state-graph routes to period-16 are **vacuous**. | Robust observation | `docs/experiment-logs/2026-08-19-orbit-cycle-structure.md`, `data/wedge/orbit_cycle_structure.json`. Floyd on the real seed orbit exits after 52805 steps at a zero word, no cycle. 4096 random starts follow the geometric law `(1-2^-16)^n` across four orders of magnitude. A cycle would need a trajectory to survive ~2.15e9 steps: `P ~ 10^-14231`. | Nothing - the route is closed. The surviving route is the walk to `d ~ 10^6` resolving each branch point. |
| The single-seed light cone splits into a settled wedge of area fraction ≈0.373 and a not-yet-settled core of ≈0.627, with no intermediate regime. | Robust observation | `docs/experiment-logs/2026-08-15-structured-wedge-decomposition.md`, `data/wedge/wedge_profile_T65536.json`, `experiments/wedge_profile.py`. **Independently replicated** at `s=1.3411` vs `1.3389` by a second agent from spec through the repo's own verified kernels (`experiments/wedge_verify.py`), all gates PASS. | Push `T` past 10^5 to pin the slow upward drift in the constant. |
| The settled wedge has an `O(t)`-size description with `O(1)` random access, via the O(1) pattern map `(w_{d-2}, w_{d-1}) ↦ w_d` on 16-bit words. | **Certificate** | Verifier: `python experiments/diagonal_recursion.py --steps 30000 --diagonals 15000 --pretty` (~24 s, exits non-zero on failure). Reproduces **276,326,150** settled cells from a 29.3 KiB description with **0 mismatches** (1151x). Artifact `data/wedge/diagonal_recursion_T30000.json`. | Raise `T` and confirm the compression ratio keeps scaling linearly. |
| Left-edge structure is **disjoint from the prize object**: `settle(T) ≈ 1.34·T > T`, so the center column is never in the settled region at any horizon. Hence left-edge regularity cannot give a sub-quadratic center-column algorithm. | Robust observation | Settling law + pattern map above; the unsettled core has area `≈0.627·t²` and is indistinguishable from i.i.d. fair coins under periodicity, block entropy, and zlib vs a matched control. Supersedes an earlier, weaker "≈1.60× constant factor" phrasing. | Extend the incompressibility argument from these three estimators to a named model class with a counting bound, turning the ceiling into a certificate. |
| The 10M center-column bitstream is reproduced bit-exactly by the current (post-2026-06-fix) kernel, and is hash-anchored. | **Certificate** | Verifier: `python tools/verify_data.py --bitstream data/center_col_10M.bin` (exits non-zero on failure). Regenerated on the fixed kernel and compared byte-for-byte against the March 2026 artifact: sha256 `6f8670b4a89826c8228d6a165047792e91551dedfb2853b8f12572d466b7547e` both sides, **0 of 10,000,000 bits differ**. Also agrees with the independent `tools/gen_golden_reference.py` over its first 1,000,000 bits. Hashes anchored in `data/MANIFEST.sha256` (166 verified, 0 absent). This closes the March-vs-June gap in `docs/DATA_INTEGRITY.md` in the passing direction. | Extend the golden reference past 1M bits so the independent check covers the whole stream, not its first 10%. |
| ~~No LSTM, CNN or Transformer at any scale finds exploitable structure in the center column (README I-L).~~ | **Retracted 2026-08-29** (was: Observation) | `experiments/lstm_prediction.py:36`, `cnn_nonstationarity.py:31`, `transformer_prediction.py:40`, `scaling_laws_ml.py:51` all call `np.unpackbits` without `bitorder`, defaulting to MSB-first, while `gpu/rule30_sim.py:226` writes LSB-first. Those four models were therefore trained on the center column **with every consecutive 8-bit block reversed** (499,516 of 1,000,000 bit positions differ; verified `MSB-decode == per-byte reversal`). The bit mean is identical either way (0.500768), which is why no aggregate check caught it. A-H pass `bitorder='little'` and are unaffected. | Re-run all four with `bitorder='little'`. The reversal is deterministic, bijective and position-local, so it permutes structure within bytes rather than destroying it and the conclusions may well survive - but that must be shown, not assumed. |
| No shortcut for the nth center bit is known in this repo. | Observation | Negative Markov/ML/coarse-grain experiments; LFSR route closed by `S_linear_complexity.md`. | A `prize_lab.py` shortcut-search harness that emits candidate programs/transducers or checkable negative artifacts. |

## Promotion Rule

When adding a new result, state the next promotion step explicitly. If there is
no plausible promotion path, the experiment is probably not worth expanding.
