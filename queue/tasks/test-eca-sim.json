{
  "mode": "test",
  "task": "Add unit tests for experiments/eca_sim.py in a NEW file tests/test_eca_sim.py.\n\nCover: step_naive_rule, step_packed_cpu_rule and simulate_spacetime_rule_cpu. Prove the packed and naive steppers agree bit for bit on rule 30 from a single-black-cell row, and pin rule 30's first few center-column bits plus the trivial rules 0 and 255 as controls.\n\nRead the module first; do not guess its API. Test only pure functions that need no data file: the canonical bitstreams (data/center_col_*.bin) are gitignored and ABSENT here, so a test that opens one is a test that cannot run. If a function needs bits, build them in the test with the module's own small CPU simulator or a literal array. Never call np.unpackbits without bitorder='little' -- tools/lint_bitorder.py fails the build on a bare call. Do not import cupy or torch and do not require a GPU; use unittest.skipUnless for a GPU-only path. Do not edit the experiment module itself, do not touch docs/STATUS.md or docs/CLAIM_LEDGER.md, and create ONLY the one new test file named below. Assert real values you derived by running the code -- not tautologies like assertIsNotNone.\n\nDone looks like: `python -m unittest tests.test_eca_sim` passes, and `python tools/lint_bitorder.py` still passes.",
  "context_files": [
    "experiments/eca_sim.py",
    "tools/lint_bitorder.py",
    "CLAUDE.md"
  ],
  "acceptance": [
    "python -m unittest tests.test_eca_sim",
    "python tools/lint_bitorder.py"
  ],
  "budget_minutes": 25,
  "tools": [
    "list_dir",
    "read_file",
    "grep",
    "write_file",
    "edit_file",
    "run"
  ],
  "limits": {
    "max_turns": 40,
    "max_tool_calls": 80,
    "max_cost_usd": 0.35,
    "wall_clock_s": 1500
  }
}
